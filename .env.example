# Leave Cognito + API unset to use local mock auth + mock control plane.
# After `cd infra && npx cdk deploy`, fill from stack outputs:

PUBLIC_BUZZ_API_URL=
PUBLIC_COGNITO_USER_POOL_ID=
PUBLIC_COGNITO_CLIENT_ID=
PUBLIC_COGNITO_REGION=us-west-2
PUBLIC_COGNITO_DOMAIN=

# Optional marketing analytics (GA4 measurement ID, e.g. G-XXXXXXXXXX)
PUBLIC_GA_MEASUREMENT_ID=
