# After `cdk deploy BuzzAuthStack`, copy the stack outputs here
# (and into GitHub Actions / marketplace build vars).
#
# The previous pool us-west-2_MIDcSvkwq / client 5klcs2rg7lmfgjggsl7bo2llkm
# was deleted with BuzzStack (2026-09-07). Do not use those IDs.
#
# PUBLIC_COGNITO_USER_POOL_ID=
# PUBLIC_COGNITO_CLIENT_ID=
# PUBLIC_COGNITO_REGION=us-west-2
# PUBLIC_COGNITO_DOMAIN=
#
# HTTP API died with BuzzStack. Leave unset until a slim API is added.
# PUBLIC_BUZZ_API_URL=
